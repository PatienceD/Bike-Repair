import React from "react";
import "./Header.css";
const Header = props => (
    <div className="row">
        <div className="header">

            {/* Welcome {FirstName} */}
    </div>

    </div>
);

export default Header;